{
  "name": "new10sapp",
  "version": "1.0.0",
  "description": "new10s staff application",
  "main": "index.js",
  "scripts": {
    "build": "echo \"No build script specified\"",
    "cap:sync": "cap sync",
    "cap:copy": "cap copy",
    "cap:open": "cap open"
  },
  "dependencies": {
    "@capacitor/android": "^6.2.0",
    "@capacitor/core": "^6.2.0"
  },
  "devDependencies": {
    "@capacitor/cli": "^6.2.0"
  }
}
